Needed installed:

xelatex : needed for thesis

Fonts:
    $ mkdir ~/.fonts
    $ cp fonts/IPAexfont00401.zip ~/.fonts

    $ cd ~/.fonts
    $ unzip IPAexfont00401.zip
    $ mv IPAexfont00401/*ttf .

    $ fc-cache -fv

    $ rm -rf ~/.fonts/IPAexfont00401*


