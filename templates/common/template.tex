%%%%%%%%%%%%%%%%%%%%%%%%%% templates/common/template.tex %%%%%%%%%%%%%%%%%%%%
\usepackage{microtype}
\usepackage[utf8]{inputenc}
% \usepackage[T1]{fontenc} removed due to wrong font encoding

\makeatletter
\@ifpackageloaded{cleveref}{}{\usepackage{cleveref}}
\@ifpackageloaded{graphicx}{}{\usepackage{graphicx}}
  \graphicspath{{./}{figures/}{auto}$if(graphicspath)$$for(graphicspath)${$graphicspath$}$endfor$$endif${Figure}{examples/documentation/images}{images}}%
\AtBeginDocument{
\usepackage[normalem]{ulem}
}
\makeatother

%%arabluatex incompatible with snat template
%%\usepackage[novoc]{arabluatex} % default options are "novoc" and "fdf2alif" other options are "voc", "fullvoc", "utf", "trans" and "fdf2noalif"
%%% arabxetex loads bidi.sty and fontspec.sty
%%\newfontfamily\arabicfont[Script=Arabic,Scale=1.3,WordSpace=1.3]{Amiri}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newlength{\cslentryspacingunit} % times entry-spacing
\setlength{\cslentryspacingunit}{\parskip}
\newenvironment{CSLReferences}[2] % #1 hanging-ident, #2 entry spacing
 {% don't indent paragraphs
  \setlength{\parindent}{0pt}
  % turn on hanging indent if param 1 is 1
  \ifodd #1
  \let\oldpar\par
  \def\par{\hangindent=\cslhangindent\oldpar}
  \fi
  % set entry spacing
  \setlength{\parskip}{#2\cslentryspacingunit}
 }%
 {}
\usepackage{calc}
\newcommand{\CSLBlock}[1]{#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{#1}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{#1}\break}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$

$if(header-includes)$
$header-includes$
$endif$
%%%%%%%%%%%%%%%%%%%%%%%%%%!templates/common/template.tex %%%%%%%%%%%%%%%%%%%%
